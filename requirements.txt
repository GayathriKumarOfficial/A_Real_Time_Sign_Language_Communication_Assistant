# ─── Core ML ──────────────────────────────────────────────
tensorflow==2.15.0
keras==2.15.0
numpy==1.24.3
scikit-learn==1.3.2
pandas==2.1.4
h5py==3.10.0
joblib==1.3.2
tqdm==4.66.1

# ─── Computer Vision & Landmarks ──────────────────────────
opencv-python==4.8.1.78
mediapipe==0.10.9
protobuf==3.20.3

# ─── Visualization (training/analysis scripts) ────────────
matplotlib==3.8.2
seaborn==0.13.0

# ─── Web Framework ────────────────────────────────────────
flask==3.0.0
flask-socketio==5.3.6
eventlet==0.33.3
python-socketio==5.11.0

# ─── Speech (standalone scripts only) ────────────────────
# SpeechRecognition and pyaudio used by speech_to_text.py standalone
# The web app hearing side uses browser Web Speech API (no server needed)
SpeechRecognition==3.10.0
pyaudio==0.2.14
pyttsx3==2.90

# ─── Whisper (standalone only, requires ffmpeg on PATH) ───
# Only needed if running speech_to_text.py directly
# Web app does NOT use Whisper — browser handles transcription
openai-whisper>=20231117